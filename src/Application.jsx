import { Header, Form, List } from './components';

const Application = () => {
    return (
        <div className={'container'}>
            <Header />
            <Form />
            <List />
        </div>
    );
}

export default Application;
